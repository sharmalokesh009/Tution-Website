import React from "react"

export default function Loading(props){
    return <div className="loading-container" style={{marginTop:props.margintop}}>
        <div className="loading-icon">
        </div>
    </div>
}